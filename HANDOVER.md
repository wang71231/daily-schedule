# 每日课表项目交接文档

## 1. 项目概况

这是一个发布在 GitHub Pages 上的手机端课表网页，包含两套互不覆盖的课表：

- 大学课程表：按教学周、星期和第几节显示。
- 体培课程表：按实际日期和具体时间显示，并支持课时与工资记录。

GitHub 仓库：`https://github.com/wang71231/daily-schedule`

线上入口：`https://wang71231.github.io/daily-schedule/`

## 2. 目录和页面

- `index.html`：课表首页，用于选择两套课表。
- `university.html`：原大学课表，保留原有教学周、单双周和日期切换功能。
- `training.html`：体培课表，包含课时记录、历史课时补录和临时加课。
- `manifest.webmanifest`：PWA 配置，可添加到手机主屏幕。
- `sw.js`：Service Worker，负责缓存和离线访问。更新页面后需要递增 `CACHE_NAME`。
- `icon.svg`、`icon-192.png`、`icon-512.png`：网页和 PWA 图标。

## 3. 体培固定课程

当前固定安排来自 2026 下半年体培进校及俱乐部排课表，老师为胡翊铭：

- 周一 15:30–17:00：嘉定江桥小学。
- 周二 16:00–17:30：中山北路第一小学。
- 周三 15:45–17:30：复旦德富路小学。
- 周四 15:50–17:00：复旦徐汇实验小学。
- 周五 09:45–10:45：嘉秀幼儿园。
- 周五 14:10–15:40：嘉城学校二年级校队（山羊馆）。
- 周五 16:30–17:20：悦庭幼儿园（延时课）。
- 周日 09:00–12:00：幸城学校 4–5 年级校队。
- 周日 15:00–18:00：体殷精英 2 队。

固定课程从表中对应的起始日期开始显示，不会在起始日期前显示。

## 4. 课时和工资记录

在体培页面的“课时记录”区域：

- 每节课可标记为“未记录、已完成、请假、取消”。
- 只有“已完成”的记录计入课时和工资。
- 实际课时可以手动修改，适合处理提前结束、延时或实际出勤变化。
- 可填写每小时工资，页面自动计算本周课时、本月课时和累计工资。
- “9 月 15 日前历史课时”用于补录旧课时，计入 9 月和累计数据。
- “导出记录”会下载 `体培课时记录.csv`，包含日期、地点、来源、状态、实际课时和工资。

## 5. 临时加课

展开“添加临时课程”，填写日期、课程名称、地点、开始时间和结束时间后保存。

临时课程会：

- 出现在对应日期的上方日程卡片中，并标记为“临时”。
- 出现在当天课时记录中。
- 可以标记完成、修改实际课时并计入工资。
- 可以从当天的临时课程列表中删除。
- 在导出 CSV 时标记来源为“临时加课”。

## 6. 数据保存位置

课时数据保存在当前浏览器的 `localStorage` 中：

- `trainingLogs`：每日固定课程和临时课程的出勤、实际课时记录。
- `extraTrainingCourses`：临时加课信息。
- `historyHours`：9 月 15 日前历史课时总数。
- `trainingRate`：每小时工资。

这些数据不会自动同步到 GitHub 或其他设备。清理浏览器网站数据、换浏览器或换手机可能导致记录消失，因此定期导出 CSV 备份。

## 7. 常用操作

打开根网址后，先选择“大学课程表”或“体培课程表”。页面内的“退出课表”按钮会返回首页，不会删除任何记录。

手机显示旧版本时，先刷新页面；若仍未更新，关闭页面后重新打开带有最新查询参数的网址，或清除该网站缓存后再访问。

## 8. 修改和发布

本地工程目录：`C:\Users\wrh\Documents\Codex\2026-07-25\new-chat-2\daily_schedule\`

修改后建议：

1. 检查 `training.html` 和 `university.html` 的脚本语法。
2. 检查手机宽度约 390px 时没有横向滚动。
3. 修改 `sw.js` 的 `CACHE_NAME`，例如从 `v11` 改为 `v12`。
4. 提交并推送到 GitHub：

```text
git add .
git commit -m "说明本次修改"
git push origin main
```

当前最新功能提交：`36e6405` 之后继续加入了返回首页、历史课时、临时加课显示和记录功能。

## 9. 重要注意事项

- 不要把 `university.html` 和 `training.html` 合并，否则会覆盖其中一套课表。
- 修改固定课程时，同时检查 `date`、`day`、`time` 和 `start` 的对应关系。
- 临时课程使用独立的 `extraTrainingCourses` 存储，不要直接写入固定课程数组。
- `清空记录`会清空每日课时记录，但当前实现不会自动清除历史课时和临时课程；执行前仍应先导出 CSV。
